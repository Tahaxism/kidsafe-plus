<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Device Admin receiver — needed for remote-lock. -->
        <receiver
            android:name=".admin.KidsafeDeviceAdminReceiver"
            android:label="KidSafe+ Admin"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/kidsafe_device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <!-- Accessibility service — used to detect foreground app and overlay
             a "blocked" screen when the child opens a forbidden app. -->
        <service
            android:name=".services.AppBlockerAccessibilityService"
            android:label="KidSafe+ App Blocker"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/kidsafe_accessibility_service" />
        </service>

        <!-- Foreground service that keeps the accessibility logic running
             after device reboot. -->
        <receiver
            android:name=".boot.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
